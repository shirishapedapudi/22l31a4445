import React from 'react';
import ReactDOM from 'react-dom/client';
import Stats from './App'; // or wherever you saved above

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Stats />
  </React.StrictMode>
);
